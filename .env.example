# Local proxy for ccxt to reach overseas exchanges.
# Leave blank if you're outside mainland China.
# 国内用户填本地代理地址 (Clash Verge 默认 7890, Shadowsocks 默认 1080 等)。
HTTPS_PROXY=http://127.0.0.1:1082
HTTP_PROXY=http://127.0.0.1:1082
